{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y git git-lfs curl && git lfs install && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}'"
  }
}
